import { ProfileShell } from '@/components/digital/profile/ProfileShell'
export default function Layout({ children }: { children: React.ReactNode }) {
  return <ProfileShell>{children}</ProfileShell>
}
